import type { AssetSurfaceCredentialBinding } from "@t3tools/client-runtime/state/assets";

export const WEB_ASSET_SURFACE_CREDENTIAL_BINDING =
  "native-header-or-cookie" satisfies AssetSurfaceCredentialBinding;
