# T1 Desktop Launch WIP Preservation

Source: `/home/adam/wt-t1-desktop-launch` dirty worktree on branch `t1-desktop-launch`.

Current main checked for overlap: `origin/main` at `930c6e811`.

Relevant merged PRs found while triaging: #41 desktop Cloudflare Access remote pairing, #46 desktop launch smoke gate, #65 packaged launch gate hardening. Those cover parts of the theme, but the exact seven-file WIP still contains unmerged material, so this branch preserves the exact diff off-box as a patch artifact without forcing it into product code.
